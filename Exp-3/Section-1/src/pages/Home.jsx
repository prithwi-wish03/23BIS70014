import React from 'react';

const Home = () => {
    return (
        <div className="page-container">
            <div className="glass-card">
                <h1>Welcome Home</h1>
                <p>
                    This is a simple Single Page Application demonstrating basic client-side routing using React Router DOM.
                    Navigate through the links above to see rotation between different views without page reloads.
                </p>
            </div>
        </div>
    );
};

export default Home;
